import { FirstPartBlock } from "../../widgets";
import React from "react";

export const Page = () => {
    return (
        <div className="page align_center">
            <FirstPartBlock />
        </div>
    );
};
